document.addEventListener('DOMContentLoaded', function () { document.body.classList.add('fade-in'); let date = new Date().getFullYear(); document.getElementById('year').textContent = date; const magnifyCard = document.getElementById("magnify-card"); const magnifyIcon = magnifyCard.querySelector("i"); magnifyCard.addEventListener("mouseenter", () => { magnifyIcon.classList.add("fa-fade"); }); magnifyCard.addEventListener("mouseleave", () => { magnifyIcon.classList.remove("fa-fade"); }); const dropletCard = document.querySelector(".feature-card--2"); const dropletIcon = dropletCard.querySelector("i"); dropletCard.addEventListener("mouseenter", () => { dropletIcon.classList.add("fa-beat-fade"); }); dropletCard.addEventListener("mouseleave", () => { dropletIcon.classList.remove("fa-beat-fade"); }); const toggleCard = document.getElementById("toggle-card"); const toggleIconContainer = document.getElementById("toggle-switch"); const toggleIcons = ['<i class="fas fa-sliders-h" style="transform: rotate(0deg);"></i>', '<i class="fas fa-sliders-h" style="transform: rotate(180deg);"></i>']; let currentToggleIndex = 0; let toggleInterval; toggleCard.addEventListener("mouseenter", () => { toggleInterval = setInterval(() => { currentToggleIndex = (currentToggleIndex + 1) % toggleIcons.length; toggleIconContainer.innerHTML = toggleIcons[currentToggleIndex]; }, 1000); }); toggleCard.addEventListener("mouseleave", () => { clearInterval(toggleInterval); currentToggleIndex = 0; toggleIconContainer.innerHTML = toggleIcons[currentToggleIndex]; }); const plantCard = document.getElementById("plant-card"); const plantIconElement = document.getElementById("plant-icon"); const plantIcons = ["🌱", "🌿", "🌾", "🌵", "🌳"]; let currentPlantIndex = 0; let plantInterval; plantCard.addEventListener("mouseenter", () => { plantInterval = setInterval(() => { currentPlantIndex = (currentPlantIndex + 1) % plantIcons.length; plantIconElement.textContent = plantIcons[currentPlantIndex]; }, 1000); }); plantCard.addEventListener("mouseleave", () => { clearInterval(plantInterval); }); const chartCard = document.getElementById("chart-card"); const chartIcon = chartCard.querySelector("i"); chartCard.addEventListener("mouseenter", () => { chartIcon.style.animation = "chartRubber 1s infinite"; }); chartCard.addEventListener("mouseleave", () => { chartIcon.style.animation = ""; }); const syncCard = document.querySelector(".feature-card--8"); const syncIcon = syncCard.querySelector("i"); syncCard.addEventListener("mouseenter", () => { syncIcon.classList.add("fa-spin"); }); syncCard.addEventListener("mouseleave", () => { syncIcon.classList.remove("fa-spin"); }); const bellCard = document.querySelector('.feature-card--5'); const bellIcon = bellCard.querySelector("i"); bellCard.addEventListener("mouseenter", () => { bellIcon.classList.add("fa-bounce"); }); bellCard.addEventListener("mouseleave", () => { bellIcon.classList.remove("fa-bounce"); }); const clockCard = document.getElementById("clock-card"); const clockIcon = clockCard.querySelector("i"); clockCard.addEventListener("mouseenter", () => { clockIcon.style.animation = "clockPulse 1s infinite"; }); clockCard.addEventListener("mouseleave", () => { clockIcon.style.animation = ""; }); });
